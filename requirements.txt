# Python 3.9+
# build_continuous / compute_factors / validate_factors / ic_check are pure
# pandas and run fully offline on any daily panel. requests is needed only by
# fetch_futures_panel.py, which delegates to the sibling skill-pandadata-api.
pandas>=1.3
numpy>=1.20
# optional
# pyarrow>=8.0         # parquet I/O for large panels
